import styled from 'styled-components';

const Review = styled.div`
  font-style: italic;
`;
const ReviewTitle = styled.h2`
  font-style: normal;
  text-decoration: underline;
`;

export { Review, ReviewTitle };
